version: "3.9"

services:
  app:
    image: dev.chat-ws-server:latest
    container_name: dev.chat-ws-server

    build:
      context: .
      dockerfile: Dockerfile
      args:
        PORT: ${PORT}

    ports:
      - "${PORT}:${PORT}"

    env_file:
      - .env

    restart: unless-stopped

    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 5
 